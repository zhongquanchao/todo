# TODO v1.0.0 · 升级交付说明

由「悬浮待办」（v0.3.1）升级为 **TODO**。应用内标识（bundle identifier）与本地数据 key 均保持不变，**旧待办数据无损保留**。

## 一、本次改动

### 数据模型（保留旧数据）
- 旧 `itemsByDay`（今天/明天/后天三段式）迁移为扁平 `items`（绝对日期 `due` + `project` + `doing` + `followUp`）。
- 加载时 `migrateState()` 自动无损升级；旧「今天/明天/后天」分别映射到对应绝对日期。
- localStorage key（`floating-todo/snapshot`）与 identifier（`com.gaopengfei.floating-todo`）保持不变，升级后数据自动衔接。

### 核心能力
- **四视图**：收件箱（无日期）/ 今天 / 未来三天（明天·后天·大后天）/ 项目，各视图显示未完成数量。
- **自然语言**：保留「今天/明天/后天/15:00/下午3点半」全部解析，新增 `大后天` 与 `#项目名`。
  - `大赛发榜 #创意大赛` → 标题「大赛发榜」、归入「创意大赛」项目。
- **项目**：新建 / 切换 / 归档 / 删除（删除时待办移回收件箱，不丢数据）；项目内待办全部完成后**自动归档**，出现新未完成则**自动恢复**。
- **工作流**：完成 / 删除（二次确认防误触）/ 编辑标题、描述、项目、进行中、跟进日期。
- **滚动**：面板高度缩小时待办列表纵向滚动，底部待办不丢失。

### 悬浮球
- 点标题栏「－」收起为**玻璃水滴小球**（独立小窗口，单层毛玻璃、无厚重圆环、无呼吸脉冲）。
- 小球显示**收起前当前视图**的未完成数（收件箱 2 → 2；未来三天 6 → 6，不会错误显示「今天」数量）。
- 小球可拖到主屏/扩展屏/不同 DPI 屏幕；点击小球**以其当前位置为锚点**展开面板，并夹紧在球所在显示器内。

### 窗口
- 标题栏拖动（支持触摸/多显示器）、边缘缩放、设置里**尺寸预设**一键调整。

## 二、测试与校验

- 单元测试：`tests/` 共 **37 项**，`npm test` 全部通过。覆盖：时间解析、项目解析、项目自动归档/恢复、视图计数与滚动窗口、悬浮球计数（收件箱/未来三天/项目）、多显示器展开定位（含负坐标扩展屏夹紧）、滚动 CSS 结构。
- 语法检查：`node --check` 通过（core.js / main.js / ball.js）。
- Rust 后端：`src-tauri/src/lib.rs` 新增 `collapse_to_ball` / `expand_from_ball` 命令（双窗口协调 + 锚点展开 + 显示器夹紧）。

## 三、构建与产物

- 本机为 Windows、无 Rust 工具链；**macOS `.dmg` 无法在 Windows 上本地构建**，由 GitHub Actions（`.github/workflows/release.yml`，macos-latest 矩阵）在打 `v*` tag 时自动产出 `TODO_x.x.x_aarch64.dmg` 与 `TODO_x.x.x_x64.dmg`。
- 或在一台 Mac 上执行：`npm install && npm run build`。
- 安装/升级/卸载（含「先清旧版同名应用」）见 [INSTALL.md](./INSTALL.md)。

## 四、主要文件

- `src/core.js` — 纯逻辑（解析/视图/项目/迁移/展开定位），可单测。
- `src/main.js` — 前端视图与交互。
- `src/ball.html` + `src/ball.js` — 悬浮球窗口。
- `src/styles.css` — 毛玻璃质感、深浅主题、悬浮球样式。
- `src-tauri/src/lib.rs` — Rust 后端（双窗口/悬浮球命令/托盘）。
- `tests/` — Node 单元测试。
- `INSTALL.md` — 安装/升级/卸载说明。
